import { PokemonTypePillDirective } from './pokemon-type-pill.directive';

describe('PokemonTypePillDirective', () => {
  it('should create an instance', () => {
    const directive = new PokemonTypePillDirective();
    expect(directive).toBeTruthy();
  });
});
